# Packages

Each package is an integration profile. Profiles reference upstream API specs
and add the curation a raw spec usually lacks: auth hints, safety policy,
operation output hints, and workflow recipes.

For now this directory contains a single GitHub profile to prove the APIs.guru
bootstrap path.
